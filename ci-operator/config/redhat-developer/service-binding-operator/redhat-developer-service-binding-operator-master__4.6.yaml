base_images:
  os:
    name: ubi
    namespace: ocp
    tag: "7"
binary_build_commands: make build
build_root:
  project_image:
    dockerfile_path: openshift-ci/Dockerfile.tools
resources:
  '*':
    requests:
      cpu: 100m
      memory: 250Mi
tag_specification:
  name: "4.6"
  namespace: ocp
tests:
- as: acceptance
  steps:
    cluster_profile: aws
    test:
    - as: test
      cli: latest
      commands: OPERATOR_REPO_REF=$(cat /usr/local/quay.credentials | cut -d "=" -f1) QUAY_USERNAME=$(cat /usr/local/quay.credentials | cut -d "=" -f2) QUAY_TOKEN=$(cat /usr/local/quay.credentials | cut -d "=" -f3) VERBOSE=2 make -k release-operator test-acceptance-with-bundle test-acceptance-artifacts
      credentials:
      - mount_path: /usr/local/quay.credentials
        name: redhat-developer-services-binding-operator-test-quay-credentials
        namespace: ocpappsvc-secrets
      from: src
      resources:
        requests:
          cpu: 100m
    workflow: ipi-aws
- as: validate-master-build
  cron: 0 0 * * *
  steps:
    cluster_profile: aws
    test:
    - as: test
      cli: latest
      commands: VERBOSE=2 make -k test-acceptance-with-bundle test-acceptance-artifacts
      from: src
      resources:
        requests:
          cpu: 100m
    workflow: ipi-aws
zz_generated_metadata:
  branch: master
  org: redhat-developer
  repo: service-binding-operator
  variant: "4.6"
